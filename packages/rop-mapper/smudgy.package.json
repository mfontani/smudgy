{
    "name": "rop-mapper",
    "version": "0.2.0",
    "description": "Rites of Passage mapper using authoritative Room.Info exits and Room.Map neighborhoods, with explicit cloud upgrade.",
    "dependencies": [
        "smudgy://official/auto-mapper@^0.3.5"
    ],
    "hosts": [
        "play.ropmud.com"
    ],
    "permissions": {
        "smudgy": {
            "interop": ["read"],
            "mapper": ["write"],
            "automations": ["aliases"],
            "session": ["echo"],
            "gmcp": ["send"]
        }
    }
}
